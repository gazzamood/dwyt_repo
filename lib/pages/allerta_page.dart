import 'package:flutter/material.dart';

class AllertaPage extends StatelessWidget {
  const AllertaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Allerta'),
      ),
      body: const Center(
        child: Text('Pagina Allerta'),
      ),
    );
  }
}
